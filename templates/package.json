{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "scripts": {
    "dev": "gulp --silent",
    "prod": "gulp --silent -p"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.3",
    "balm": "^2.0.4",
    "gulp": "^4.0.2"
  },
  "private": true
}
